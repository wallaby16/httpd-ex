node('') {
  stage 'build'
  openshiftBuild(namespace: 'httpd', buildConfig: 'httpd-example', showBuildLogs: 'true')
  stage 'deploy'
  openshiftDeploy(namespace: 'httpd', deploymentConfig: 'httpd-example')
}
